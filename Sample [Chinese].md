[#] 範例
- 參考資料
   - [Ray-Guang Cheng and Chung-Ju Chang, "Design of a fuzzy traffic controller for ATM networks," in IEEE/ACM Transactions on Networking, vol. 4, no. 3, pp. 460-469, June 1996](https://ieeexplore.ieee.org/stamp/stamp.jsp?tp=&arnumber=502244)
   - Prompt: 讀這份論文，簡要列出：
## A1. 專題摘要
請用 150～300 字說明
- 要解決的問題: 
- 此問題的挑戰
- 預計使用的方法，以及
- 最後要完成的成果。

## A2. 系統架構圖
定義
- 系統假設
- 環境
- 輸入、輸出參數
用系統方塊圖標示
- 環境
- 預定的控制模組
- 輸入、輸出參數
---

## 基本資料

| 欄位 | 內容 |
|---|---|
| 專題名稱 |  |
| 學號／姓名 |  |
| Git／作品連結 |  |
| 規劃確認日 | YYYY-MM-DD（經兩位教師確認後開始計分） |

---

# Part A　專題細部規劃

## A1. 專題摘要
請用 150～300 字說明
- **要解決的問題：**ATM 網路必須同時解決兩個相互依存的問題：壅塞控制（Congestion Control）與通話允入控制（Call Admission Control, CAC）。傳統方法各自處理其中一個，無法整合，且依賴難以取得的精確流量統計參數。
- **此問題的挑戰**：多媒體流量具有高度突發性（bursty），統計特性難以事先掌握；動態設定壅塞門檻極為困難；允入決策必須在不完整資訊下即時完成，且需同時滿足封包遺失率 ≤ 10⁻⁵ 的 QoS 限制。
- **預計使用的方法**：設計一個整合型模糊流量控制器（Fuzzy Traffic Controller），含三個協同模組：模糊壅塞控制器（FCC）、模糊頻寬預測器（FBP）、模糊准入控制器（FAC）。採用模糊推論處理不確定性，並以遺傳演算法（GA）自動最佳化隸屬函數形狀與控制規則，減少人工調參需求。
- **最後要完成的成果**：整合壅塞控制與 CAC 的單一控制架構。模擬結果顯示：相較等效容量法，系統利用率提升 約 11%；相較雙門檻法，壅塞控制性能提升約 4%，在維持 QoS 的前提下，同時改善兩個控制問題的表現。

## A2. 系統架構圖
請定義
- **系統假設**：
   - 兩種流量類型：Type-1 即時型（語音、視訊）、Type-2 非即時型（資料）
   - 各設獨立有限緩衝區 K₁、K₂
   - 系統容量 C，為 Type-1 保留容量 Cᵣ
   - QoS：封包遺失率 ≤ 10⁻⁵
- **環境**：
   - 用戶端（CPE）：含預緩衝區，接受准入決策後調整傳輸速率
   - 網路端（ATM 交換節點）：含輸出緩衝區與輸出鏈路，是壅塞發生的位置
- **輸入參數**：
   - Rₚ 峰值位元率
   - Rₘ 平均位元率
   - Tₚ 峰值持續時間
   - q 佇列長度
   - Δq 佇列變化率
   - pₗ 封包遺失率
   - Cₐ 可用容量
- **輸出參數**：
   - Cₑ 估計等效容量
   - y 壅塞控制動作（DM / DS / NC / IS / IM 五級）
   - z 允入決策（Accept / WA / WR / Reject）

用[系統方塊圖](https://github.com/user-attachments/assets/30afb519-a782-4b61-bb98-a345e0c9929d)標示
- **環境**
- **預定的控制模組**
- **輸入、輸出參數**

## A3. 預計產出
- 驗證方式：
  - 實驗環境：電腦模擬（Discrete-event simulation）；ATM 單節點模型，總容量 C = 150 Mbps，緩衝區 K₁ = K₂ = 200 cells；流量模型：ON/OFF Markov 源（模擬突發特性）
  - 軟體版本：自製 C 語言模擬器，GA 最佳化與模糊推論均為自行實作
  - 實驗場景、設計方式與目的
     - 場景一：壅塞控制效果驗證
        - 設計方式：固定已接受連線數，逐步提高流量負載（offered load），觀察封包遺失率 pₗ 與系統吞吐量隨負載的變化曲線。
        - 比較對象：FCC vs. 雙門檻法（Two-threshold method）
        - 目的：驗證模糊控制在高突發性負載下能否維持 pₗ ≤ 10⁻⁵，並比較壅塞持續時間與恢復速度是否優於傳統固定門檻法（約 +4%）
   - 場景二：通話准入控制效果驗證
        - 設計方式：模擬動態新通話到達，由 FBP 預測等效容量 Cₑ，FAC 決定是否接受；紀錄在 QoS 滿足條件下最終被接受的通話數（系統利用率）。
        - 比較對象：FAC + FBP vs. 等效容量法（Equivalent Capacity method）
        - 目的：驗證模糊系統能否在不精確流量統計下仍達到較高系統利用率（約 +11%），同時不違反 QoS 限制

## A4. 交互驗證

- 所提貢獻是否可以解決問題所面臨的挑戰？
- 實驗結果是否可以佐證問題已經解決


驗證問題 | 分析 | 結論
-- | -- | --
所提貢獻是否可以解決問題所面臨的挑戰？ | 挑戰 1（流量突發、統計未知）：模糊推論以語言規則取代精確機率分布，無需事先知道 ON/OFF 時間分布 → 設計上可應對挑戰 2（門檻難以動態設定）：GA 自動最佳化隸屬函數與規則，免去人工調參 → 設計上可應對挑戰 3（壅塞控制與 CAC 難以整合）：FCC 輸出 y 直接送入 FAC，三模組共享 pₗ，形成協同決策單一架構 → 設計上可應對 | ✓ 是
實驗結果是否可以佐證問題已經解決？ | 場景一結果：FCC 在相同負載條件下，壅塞控制性能比雙門檻法提升 ~4%，且維持 pₗ ≤ 10⁻⁵ → 壅塞控制問題有模擬佐證場景二結果：FAC+FBP 系統利用率比等效容量法高 ~11%，QoS 不違反 → CAC 問題有模擬佐證限制：驗證僅為離散事件模擬，缺乏實體 ATM 硬體部署與大規模場景測試；流量模型（ON/OFF Markov）未必涵蓋所有真實多媒體特性 | ⚠ 部分


